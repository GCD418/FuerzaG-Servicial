@page
@model FuerzaGServicial.Pages.Reports.BrandStatisticsModel
@{
    ViewData["Title"] = "Reporte Estadístico de Marcas";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h3 class="mb-0">
                        <i class="fas fa-chart-pie"></i> Reporte Estadístico de Marcas
                    </h3>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="fas fa-exclamation-circle"></i> @Model.ErrorMessage
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <form method="post" asp-page-handler="Generate">
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label asp-for="MinCount" class="form-label fw-bold">
                                    <i class="fas fa-sort-numeric-down"></i> Cantidad Mínima de Vehículos
                                </label>
                                <input asp-for="MinCount" class="form-control" type="number" 
                                       min="0" max="10000" required />
                                <small class="form-text text-muted">Ej: 10</small>
                                <span asp-validation-for="MinCount" class="text-danger"></span>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="MaxCount" class="form-label fw-bold">
                                    <i class="fas fa-sort-numeric-up"></i> Cantidad Máxima de Vehículos
                                </label>
                                <input asp-for="MaxCount" class="form-control" type="number" 
                                       min="0" max="10000" required />
                                <small class="form-text text-muted">Ej: 30</small>
                                <span asp-validation-for="MaxCount" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">
                                <i class="fas fa-file-download"></i> Formato de Reporte
                            </label>
                            <div class="d-flex gap-3">
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" asp-for="Format" 
                                           value="pdf" id="formatPdf" checked />
                                    <label class="form-check-label" for="formatPdf">
                                        <i class="fas fa-file-pdf text-danger"></i> PDF
                                    </label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" asp-for="Format" 
                                           value="excel" id="formatExcel" />
                                    <label class="form-check-label" for="formatExcel">
                                        <i class="fas fa-file-excel text-success"></i> Excel
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div class="alert alert-info">
                            <i class="fas fa-info-circle"></i>
                            <strong>Información:</strong> Este reporte muestra estadísticas de las marcas con mayor 
                            cantidad de vehículos registrados, incluyendo un gráfico de torta para visualización.
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-page="/Index" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> Volver
                            </a>
                            <button type="submit" class="btn btn-success">
                                <i class="fas fa-chart-pie"></i> Generar Reporte
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow mt-4">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-question-circle"></i> Información del Reporte</h5>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-check text-success"></i> Muestra marcas con cantidad de vehículos y porcentajes</li>
                        <li><i class="fas fa-check text-success"></i> Incluye gráfico de torta con distribución visual</li>
                        <li><i class="fas fa-check text-success"></i> Filtrado por rango de cantidad de vehículos</li>
                        <li><i class="fas fa-check text-success"></i> Ordenado por mayor cantidad de vehículos</li>
                        <li><i class="fas fa-check text-success"></i> Disponible en formato PDF y Excel</li>
                    </ul>
                </div>
            </div>

            <div class="card shadow mt-4 border-warning">
                <div class="card-body">
                    <h5 class="card-title text-warning">
                        <i class="fas fa-lightbulb"></i> Ejemplo de Uso
                    </h5>
                    <p class="mb-2">
                        <strong>Caso:</strong> Quiero ver las marcas que tienen entre 10 y 30 vehículos registrados.
                    </p>
                    <ul class="mb-0">
                        <li>Cantidad Mínima: <code>10</code></li>
                        <li>Cantidad Máxima: <code>30</code></li>
                        <li>Resultado: Solo aparecerán las marcas con 10-30 vehículos</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
